/**
 * Database Backup Management Endpoints
 * DEPRECATED: Backup feature has been removed
 * File kept for reference only
 */

function registerBackupEndpoints(app, supabase, authenticateToken, authorizeRole) {
    // Backup endpoints disabled
    console.log('[Backup] Feature disabled - no endpoints registered');
}

module.exports = registerBackupEndpoints;
